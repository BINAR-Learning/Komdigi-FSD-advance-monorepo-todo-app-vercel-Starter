# ===============================================
# TODO: Copy file ini jadi .env dan isi dengan nilai yang sesuai
# ===============================================
#
# Cara setup:
# 1. Copy file ini: cp env.example.txt .env
# 2. Edit file .env dengan nilai yang sesuai
# 3. JANGAN commit file .env ke Git (sudah ada di .gitignore)
#
# ===============================================

# Port untuk development server (default: 3001)
PORT=3001

# Environment mode (development atau production)
NODE_ENV=development

# MongoDB Atlas Connection String
# Format: mongodb+srv://USERNAME:PASSWORD@CLUSTER.mongodb.net/DATABASE_NAME?retryWrites=true&w=majority
# 
# Cara setup MongoDB Atlas:
# 1. Daftar di https://cloud.mongodb.com (gratis)
# 2. Create cluster baru (pilih Free Tier M0)
# 3. Create database user dengan username dan password
# 4. Di Network Access, tambahkan IP 0.0.0.0/0 (allow from anywhere)
# 5. Get connection string dari cluster (klik Connect > Connect your application)
# 6. Ganti <username>, <password>, dan <database> dengan nilai yang sesuai
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/todo?retryWrites=true&w=majority

# JWT Secret Key untuk signing token (minimal 32 karakter, gunakan string random yang kuat)
# Generate dengan: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=buatlah-secret-key-yang-panjang-minimal-32-karakter-atau-lebih

# JWT Token Expiration Time (contoh: 7d = 7 hari, 1h = 1 jam, 30m = 30 menit)
JWT_EXPIRES_IN=7d

# Log Level (optional, default: info)
# Options: error, warn, info, http, debug
LOG_LEVEL=info
